'use strict'
// No cambies los nombres de las funciones.

function quickSort(array) {
  // Implementar el método conocido como quickSort para ordenar de menor a mayor
  // el array recibido como parámetro
  // Devolver el array ordenado resultante
  // Tu código:

  /*
  1. Tomar un valor del array como pivote
  2. Caso base
  3. Definir un array para los elementos menores (izquierda)
  4. Definir un array para los elementos mayores al pivote (derecha)
  5. Recorremos el arreglo
  6. Hacer la condicion para preguntar en donde estamos iterando, si el valor es menor al pivote
  7. Si cumple la condicion pusheamos al array menor (izquierda)
  8. Si no la cumple pusheamos al array mayor (derecha)
  9. Llamamos la recursion

  
  */
  
  let pivote = array[0] 
  


}

function mergeSort(array) {
  // Implementar el método conocido como mergeSort para ordenar de menor a mayor
  // el array recibido como parámetro
  // Devolver el array ordenado resultante
  // Tu código:

  //okey probemos ahora
}

// No modificar nada debajo de esta línea
// --------------------------------

module.exports = {
  quickSort,
  mergeSort,
};
